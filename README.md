# vite-tesseract-reproduction

## Reproduction

#### `yarn` 

Install dependencies

#### `yarn dev` 

It shows `vite` on page after loading

#### `yarn build` && `yarn serve` 

It throws `Uncaught ReferenceError: 
__dirname is not defined`